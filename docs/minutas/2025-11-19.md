# 2025-11-19 — Preparación del despliegue y primeros tests

- **Participantes**: Tiziano Messina
- **Canal**: Discord, avisando avances a Jeremías
- **Reconstruido de**: 15 commits del 17 al 19 de noviembre de 2025

## Temas

- Primer test automatizado con Vitest (`Added vitest`).
- Ajustes para poder desplegar: rutas relativas de Prisma en cada módulo, configuración de Turborepo,
  CORS y arreglo del logout (`Changed relative route for prisma in each /modules`,
  `Added dev variable into turbo.json`, `Changed CORS and base.ts to not move under API`,
  `Fix logout problem`, `Added navigate after logout`).

## Decisiones

- Backend y frontend se despliegan **por separado**, cada uno en el proveedor que mejor le queda, en
  vez de servir el frontend desde el backend.
- Se revierte el cambio de CORS que había roto el acceso (`Removed cors update, caused errors`):
  ante la duda, volver a lo que funcionaba y resolverlo con calma.

## Próximos pasos

- Cerrar la entrega. Quedó pendiente durante varios meses.
