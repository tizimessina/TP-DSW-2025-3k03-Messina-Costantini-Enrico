# Conexión MySQL usada por Prisma (CLI y runtime).
# Local (Docker Percona en 3307):
DATABASE_URL="mysql://agro-dsw:CAMBIAR@localhost:3307/agro-dsw"
# Producción (Aiven): agregar ?ssl-mode=REQUIRED al final de la URL.
